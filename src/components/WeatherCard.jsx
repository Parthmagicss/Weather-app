import React from "react";

const WeatherCard = ({ weatherData, addToFavorites, removeFromFavorites }) => {

  return (
    <div className="weather-card">
      {}
    </div>
  );
};

export default WeatherCard;
